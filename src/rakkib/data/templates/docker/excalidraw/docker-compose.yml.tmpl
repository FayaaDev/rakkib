services:
  excalidraw:
    image: excalidraw/excalidraw:sha-4bfc5bb
    container_name: excalidraw
    restart: unless-stopped
    networks:
      - {{DOCKER_NET}}

networks:
  {{DOCKER_NET}}:
    external: true
